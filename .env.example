# Backend API base URL (leave empty to use Vite proxy at /api during local dev)
VITE_API_BASE_URL=

# Server port
PORT=4000

# Gemini API key (needed for the AI assistant)
GEMINI_API_KEY=

# CORS allowed origin for production (set to your Vercel URL)
ALLOWED_ORIGIN=
